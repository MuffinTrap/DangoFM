#pragma once

float min(float a, float b);
float max(float a, float b);
float clamp(float low, float v, float high);

int min_int(int a, int b);
int max_int(int a, int b);
int clamp_int(int low, int v, int high);
